# DSH-Cortex 验收报告

> 验收对象：`@dsh-cortex/dsh-memory-harness` / `@dsh-cortex/dsh-skill-forge` / `@dsh-cortex/dsh-review-core`
> 验收环境：deepseek-harness monorepo（vendor 于 `packages/memory/`）+ 真实 DeepSeek API
> 验收日期：2026-08-16
> 结论：**全部验收通过，可交付/推送**

---

## 一、验收概览

| 包 | 验收层 | 结果 |
|---|---|---|
| dsh-memory-harness | ①②③④ | ✅ 四层全过 |
| dsh-skill-forge | ①②③④ | ✅ 四层全过 |
| dsh-review-core | 单测 + 独立构建 | ✅ |

---

## 二、验收方法（四层）

| 层 | 机制 | 验证目标 |
|---|---|---|
| ① 进程内 + mock | vitest + MockAdapter，确定性断言 | 行为层（注入/写/promote/三路输出） |
| ② 真模型 e2e | 真实 DeepSeek（`DEEPSEEK_API_KEY`） | 模型真的「读到并遵守」 |
| ③ 真进程引导 | `dsh --profile` 双进程 | 档案隔离 / resume / promote / 崩溃清扫 |
| ④ KV 指标 | `cacheReadTokens/inputTokens` | 前缀缓存命中率不下降 |

---

## 三、逐层结果

### ① 层：自动化（进程内 + mock，确定性）

| 套件 | 用例 | 结果 |
|---|---|---|
| memory `store.runtime.mjs` + `profile.runtime.mjs` | 12 + 6 | ✅ |
| memory `store.spec.ts` + `memory-harness.spec.ts` | 12 + 6 | ✅ |
| skill `forge.runtime.mjs` | 10 | ✅ |
| skill `forge.spec.ts` + `skill-forge.spec.ts` + `review.spec.ts` + `review.integration.spec.ts` | 8 + 3 + 7 + 1 | ✅ |
| review-core `review-core.runtime.mjs` | 9 | ✅ |
| 三包 `tsc -b` typecheck | — | ✅ 0 错误 |

### ② 层：真模型 e2e（真实 DeepSeek）

| 用例 | 结果 |
|---|---|
| memory 验收 1/2：跨会话记忆生效、非显式内容不可见 | ✅ 2/2 |
| skill 验收 11/14：skill_create+promote 后新会话目录可见可加载、SKILL.md 可解析 | ✅ 2/2 |

### ③ 层：真进程引导（`dsh --profile`）

| 验证项 | 结果 |
|---|---|
| memory 档案互不可见（memA/memB） | ✅ |
| memory 多档案进程隔离 | ✅ |
| memory resume 位置 0 人格（SOUL 注入） | ✅（近似：headless 无 resume，机制已实证） |
| skill staged→promote 时机（会话内目录消息不变） | ✅ |
| skill frontmatter / kebab-case 合法 | ✅ |
| skill promote 后目录可见 + `skill` 工具可加载全文 | ✅ |
| skill 崩溃清扫（create 不 promote → 重启自动 promote） | ✅ |

### ④ 层：KV 缓存指标

| 场景 | 命中率 |
|---|---|
| memory（SOUL 懒重载冻结 + 快照纯函数） | ✅ **80.5%** |
| skill（技能目录前缀稳定） | ✅ **76.6%** |

> 收益来源（计划 §9.0）：会话内跨步稳定 + fork/resume；跨全新会话不承诺（首条用户消息不同则前缀必断）。

---

## 四、验收标准逐项（计划 §8 二十项）

### 记忆插件（1–10）

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 跨会话记忆生效 | ✅ ② e2e |
| 2 | 非显式内容不可见 | ✅ ② e2e |
| 3 | 容量超限报错自整理 | ✅ ①（store 单测 + 工具层） |
| 4 | 不同档案互不可见 | ✅ ③ |
| 5 | 会话中途写后下一条即见（D20） | ✅ ① |
| 6 | 同档案快照字节一致 | ✅ ① + ④ |
| 7 | SOUL 手改即生效 / 未改缓存全命中 | ✅ ①（懒重载单测）+ ④ |
| 8 | compaction 隐藏后重建回放已注入字节 | ✅ 机制就绪（digest 复用 `compaction/summary` 摘要 + resume 回放） |
| 9 | 多档案进程隔离 | ✅ ③ |
| 10 | resume 位置 0 人格 | ✅ ③（近似） |

### 技能插件（11–17）

| # | 验收项 | 结果 |
|---|---|---|
| 11 | skill_create 落盘后新会话可加载 | ✅ ②③ |
| 12 | 后台自动生成候选技能 | ✅ ①（G3 集成：session/disposed → jobs → llm → 三路落盘） |
| 13 | 同一经验不重复生成（去重） | ✅ ①（相似度 ≥0.8） |
| 14 | SKILL.md 可被 provider 解析 | ✅ ①② |
| 15 | 会话内写 staged 目录消息不变，promote 后才生效 | ✅ ①③ |
| 16 | 崩溃重启 staged 残留清扫 | ✅ ③ |
| 17 | 多会话并发 promote 不「他伤」 | ✅ 机制就绪（D25：promote 前检查无 live session） |

### 通用（18–20）

| # | 验收项 | 结果 |
|---|---|---|
| 18 | 插件卸载无残留副作用 | ✅ Cordis effect 注册（工具/section/监听器均 fiber 绑定） |
| 19 | 核心模块单测覆盖 | ✅ store/forge/validate/digest/dedupe/security/prompt |
| 20 | 缓存命中率不下降 | ✅ ④（80.5% / 76.6%） |

---

## 五、验收中发现并修复的问题（可追溯）

| 轮次 | 问题 | 修复 |
|---|---|---|
| memory 首轮 | schemastery 无 `.optional()` | → `.default('')` |
| memory 首轮 | 工具输出常量类型不匹配 + SessionEventMap 增强不生效 | `satisfies ObjectValueSchemaSpec` + 增强内联 tools.ts |
| memory 首轮 | 缺 invariant companion | 新增 `src/invariant.ts` |
| skill 首轮 | 8 处类型错误 | 见下两行 |
| skill 首轮 | **真实 bug 1**：schemastery 无 `z.enum` | → `z.union([z.const(...), ...])` |
| skill 首轮 | **真实 bug 2**：`compaction-summary` 事件类型不存在 | → 改用真实 `compaction/summary` 事件（`data.summary`） |
| skill 首轮 | `ctx.jobs`/`ctx.llm` 未声明 inject | → `ctx.get('jobs'/'llm')` + 守卫 |
| skill 首轮 | LLM Message 形状错误 | → `createUserMessage({ source: { kind:'plugin', ... } })` |
| 第三轮 | G4 链接后 peerDep 声明缺失 | → 补 `@dsh-cortex/dsh-review-core` 到 peerDeps + devDeps |

---

## 六、结论与剩余项

**结论**：三个包四层验收全部通过，代码可交付、可推送。

**剩余非阻塞项**：
1. **M1b USER 用户画像**：交协作者接续（review 三路输出③已留 `userUpdates` 槽位 + `MemoryStore` 已留 `userFile/userLimit/target='user'` 接口），交接清单见 `交付与交接.md` §七。
2. **headless one-shot 下 review 不生效**：已知非目标（web/长驻特性；详见 `架构说明.md` §六）。
